{
  "name": "eldros-requirements-webhook",
  "version": "3.0.0",
  "description": "Watches deposit/requirements.md & display/requirements.md — GitHub webhook + local watcher — propagates diffs to both branches",
  "main": "watcher.js",
  "scripts": {
    "start": "node watcher.js",
    "snapshot": "node snapshot.js",
    "test-webhook": "node test-webhook.js"
  },
  "dependencies": {
    "chokidar": "^3.5.3",
    "diff": "^5.1.0",
    "dotenv": "^16.4.5",
    "mammoth": "^1.6.0",
    "xlsx": "^0.18.5"
  }
}
